language: ruby

rvm:
  - 2.1
  - 2.2
  - 2.3
  - jruby
  - rbx

env:
  - HANDLER=nokogiri
  - HANDLER=ox

matrix:
  fast_finish: true
  allow_failures:
    - rvm: jruby
  exclude:
    - rvm: jruby
      env: HANDLER=ox

before_install:
  - gem update bundler

script:
  - bundle exec rake
